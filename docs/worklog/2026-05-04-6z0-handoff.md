# 2026-05-04 — vectorfeld-6z0 handoff (cut-off mid-investigation)

The previous session (May 3 evening → May 4 08:16) was investigating
`vectorfeld-6z0` (yellow-BG composite white-margin / clipping on
Export PDF), committed WIP debug instrumentation in `df591f5`, and
was cut off before the logs from a fresh dogfood run could be
collected. This entry documents what was found post-mortem and the
recommended next move.

## What was committed (WIP)

`df591f5 wip(pdfExport): drawImage debug logs + warn on embed failure`

- `src/model/pdfExport.ts:302` — `console.log('[pdfExport] drawImage', { x, y, w, h, hasHref, hrefStart })`.
- `src/model/pdfExport.ts:319` — `console.log('[pdfExport] drawImage emit', { tlx, tly, wPt, hPt, drawY, sx, sy, ctxMatrix })`.
- `src/model/pdfExport.ts:312` — silent `catch {}` upgraded to `console.warn('[pdfExport] failed to embed image', { mime, bytes, err })`.

Commit message says: *"Strip the .log calls when the fix lands;
keep the .warn."* — that intent stands.

## What this session found (post-mortem, no code changes)

1. **Engine routing is correct.** `shouldUseGraftEngine` at
   `src/model/fileio.ts:188` returns `false` whenever
   `backgrounds.size > 0`. The composite case has the yellow-BG
   PDF registered as a background → pdf-lib path, not graft. Bead
   description (2026-04-20) blames `svg2pdf.js`; that engine is
   gone. Description is stale.
2. **Geometry of the embedded JPEG is correct.** Background PDF
   ships as `<image width="595" height="841">` in a group with
   `transform="matrix(1.0005,0,0,1.0010,0,0.0001)"`; after
   `flattenAndScalePdfLayer` prepends the PT_TO_MM scale, the
   composed matrix places it at (0, 0) covering the full page. No
   stray translation in `drawImage`.
3. **clipPath cannot be the cause.** `walk()` in `pdfExport.ts`
   (`:458–496`) has **no** `case 'clipPath'` and ignores
   `clip-path` attributes entirely. The two clipPaths in the
   background SVG are full-page no-op rects anyway. The 112-clipPath
   figure quoted in the original bead was the *foreground* PDF, not
   the background.
4. **The "white margin" likely isn't a bug.** Pixel analysis of
   `test/dogfood/composite-rendered.png` (May 3 23:03 dogfood
   output): blue border 0–3.5mm, cream/yellow continuous from
   3.5mm onward, foreground body text starts at col 110 (≈19.4mm).
   Rendering the foreground fixture standalone via `pdftoppm`
   shows the same 19.3mm leftmost-dark-pixel column — that's the
   foreground's own design margin, not a missing left strip on the
   background.
5. **Untracked diagnostics.** `test/dogfood/composite-half-1.ppm`
   and `composite-real-1.png` are manual `pdftoppm` / `mutool draw`
   renders the previous-session operator created for comparison.
   Not produced by any project script. Not committed (kept
   untracked; `composite-real-1.png` looks like a corrupted aborted
   render from the pre-`vectorfeld-249` pdfjs path and is
   discardable).

## Recommended next move

**Cheap path** (do this first):

1. `npm run dogfood:composite`. Capture the
   `[pdfExport] drawImage` and (if any) `[pdfExport] failed to
   embed image` console output the WIP was meant to gather.
2. If the JPEG embed succeeds AND the resulting PDF opens
   correctly in Preview / Adobe (not just mupdf): strip the two
   `console.log` calls in `pdfExport.ts`, keep the `console.warn`,
   close `vectorfeld-6z0` with reason "fixed by pdf-lib migration;
   original svg2pdf.js clip-path issue no longer applies."
3. If the embed silently fails: that's the real cut-off thread —
   fix the embed path before closing.

Do **not** chase coordinate offsets — pixel-level analysis already
confirmed the geometry is correct.

## State at end of session

- Working tree clean of source edits; WIP already on `main`.
- 1 in_progress bead: `vectorfeld-6z0` (kept open, notes updated
  with these findings so the next session doesn't re-discover).
- No tests run, no fix attempted. This is a documentation-only
  commit.
